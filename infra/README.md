# Infra (Terraform)

Objetivo: infraestructura AWS declarativa para `dev` y `prod`.

Recursos objetivo:
- VPC, subnets, security groups
- ECS Fargate + ALB
- RDS PostgreSQL
- ECR
- S3 + CloudFront
- IAM roles

Nota: pendiente bootstrap de módulos Terraform.
